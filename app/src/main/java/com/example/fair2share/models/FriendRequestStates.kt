package com.example.fair2share.models

// Denied is not being used because not 100% off all features are implemented yet.
// In the future you should beable to accept denied friends aswell.
enum class FriendRequestStates {
    PENDING, DENIED
}